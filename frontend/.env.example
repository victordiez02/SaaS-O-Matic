# Copiar a ".env" en este directorio solo si se quiere cambiar el valor por defecto.
# Opcional para "pnpm dev": sin este fichero, las llamadas a /api ya funcionan a
# través del proxy de vite.config.ts hacia http://localhost:8000.
# No se usa en Docker.
VITE_API_URL=http://localhost:8000/api
